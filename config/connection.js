// let db

// const createConnection = () => {
//     const mysql = require('mysql2');
//     const creds = require('./.credentials')
//     let db = mysql.createConnection(creds, console.log(`Connected to the Employee Tracker database.`));
//     return db;
// }
// const func = new Promise(createConnection)
// func.then(console.log(db))
// module.exports = createConnection